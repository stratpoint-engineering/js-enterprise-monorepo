// This file is intentionally kept in the shared package to allow
// sharing common React hooks between different frontend applications

// We export a placeholder for now, and will implement specific hooks when needed
export const useSharedHooks = () => {
  // This is a placeholder
  return {
    version: '0.1.0',
  };
};
